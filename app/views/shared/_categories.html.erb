<h1>Categorias</h1>

<ul class="categories list">
  <% Category.sorted_by_name.each do |category| %>
    <li>
      <%= link_to category.name, category_path(category) %>
      <span class="count"><%= category.questions_count %></span>
    </li>
  <% end %>
</ul>
